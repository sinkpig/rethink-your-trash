<h1>index de cooperative_materials</h1>

<% if @cooperative_materials.count == 0 %>
  <p>You still need to add the materials you accept to your list</p>
<% else %>
  <% @cooperative_materials.each do |cooperative_material| %>
    <%= cooperative_material.material.name %>
    <%= link_to "x", cooperative_cooperative_material_path(cooperative_material.user, cooperative_material), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
    <%# link_to "x", cooperative_material_path(cooperative_material), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
    <%# desnestar o destroy nas rotas
    <%# no controller direcionar os paths pro profile %> %>
  <% end %>
<% end %>

<p>Want to add new materials to your list?</p>
<%= link_to "New material", new_cooperative_cooperative_material_path %>
