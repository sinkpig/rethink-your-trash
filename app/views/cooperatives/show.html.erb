<div class="card text-center">
  <div class="card-header">
    <h1><%= @cooperative.name %></h1>
</div>

<div class="card-body">

<p class="card-text"><%= @cooperative.description %></p>

  <div class="d-grid gap-2 col-6 mx-auto">
    <button type="button" class="btn btn-primary">Trade with me</button>
    </div>
  </div>
</div>
